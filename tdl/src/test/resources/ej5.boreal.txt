{ Se debe editar el fichero para hacer las siguientes modificaciones: 
// + cambiarlo por uno de los operadores aritméticos implementados
// operador relacional: cambiarlo por uno de los operadores relacionales implementados 
// and cambiarlo por uno de los operadores lógicos implementados
// eliminar los operadores especiales no implementado
// en la función sent, dejar solamente el case o el if-else implementado y borrar la otra estructura
}

VAR
 s1: string;
 kk: integer;
 bb: boolean;
 pp: integer;
 jj: integer;


function imprime (kk: integer; s1: integer): string;
begin
	write (kk, ' # ');
	if (0 <> s1)	{ Cuando s1 valga 0, esta expresión debe evaluarse como falsa y cuando sea positivo, debe evaluarse como verdadera }
	then   write (s1);
	return ('. ');
end;

function  sent (s: string): integer;
var
	iii: integer;
	ss: string;
	kk: integer;
begin
	iii:= 8;
	jj:= 4;
	pp:= 1;
	ss:=s;
	read (ss);
	s1:=ss;
	kk:= 11;
	writeln (s, s1);
	case iii of
		1: 	begin write ('Es impar: ', iii); end;
		3: 	begin write ('Es impar: ', iii); end;
		5: 	begin write ('Es impar: ', iii); end;
		7: 	begin write ('Es impar: ', iii); end;
		9: 	begin write ('Es impar: ', iii); end;
		2:  begin iii:=0; end;
		4:  begin if (bb and (111 > iii)) writeln ('Verdad'); { cambiar la condición, pero conservando un operador relacional y un operador lógico} end;
		6: begin iii:= iii + 2; write ('Es par: ', iii); end;
		8: begin writeln (imprime ('Es par: ', iii)); end;
	end;
	if (bb and (iii < kk)) then	{ cambiar la condición, pero conservando un operador relacional y un operador lógico}
	begin
		if (bb) then writeln (imprime (iii, 0));
	end;
	else
	begin
		writeln (imprime (kk, 0));
	end;
	
	return jj;
end;

function  Recursivo (nnn: integer): integer;
var resul: integer;
begin
	kk:= kk + 11;
	writeln (imprime (nnn, kk));
	if (1 < nnn) then	return 1;	{ poner la condición para que se cumpla tras 3 o 4 iteraciones }
	resul:= nnn + Recursivo (nnn + pp);
	writeln (imprime (nnn, kk));
	return resul;
end;

function operador: integer;	{dejar solo la línea con el operador implementado}
var sss: string;
begin
	write (kk,'**3='); return kk**3);
	sss:= 'Eres ' + s1; writeln (sss); return 0;
	if (kk IN (pp, 3, 5, 7, 9 )) then return (2);
	return MIN (jj, 22, 33, kk, 44, pp);
	return MAX (jj, 8, 11, kk, 00, pp);
end;

program main;
var cinco: integer;
begin
	cinco:= 5;
	s1:= 'introduce un digito. ';
	bb:= 0 < cinco; { cambiar los valores para que bb siempre valga true en función del operador }
	write (s1);
	read (kk);
	writeln ('Escribe tu nombre. ');
	writeln (sent('Hello, '));
	writeln(operador);
	kk:=11;
	writeln (imprime (Recursivo(jj), kk));
	writeln ('Bye, ', (s1));
end;
var x : integer;

procedure cositas (var y: integer; var z: string);
begin
	write('[+] Imprimimos la cadena pasada como parametro: ');
	writeln(z);
	read(z);
	writeln(z);
	if (y = 2) then
		begin
			x := y * 5;
		end;
	else
		begin
			x := y * 4;
		end;
	return;
end;

PROGRAM pruebaOperadores;

var r : integer;
	y : integer;
	b : boolean;
	d : boolean;
	s : string;

begin
	y := 2;
	writeln(y);

{    x := 4 * 3;
	writeln(x);
    
    b := x = y;

	b := b AND d;

	write('[+] Calculamos el minimo de una lista y lo imprimimos: ');
	r := min(1970, 2001, 1917, 1939);
	if r = 1917 then begin writeln(r); end;

	write('[+] Llamamos a whichcad e imprimimos el resultado: ');
	s := whichcad(y);
	writeln(s);
}
	s := 'hola';
	cositas(y, s);
	writeln(x);
	writeln(s);

	write('[+] Imprima una cadena por pantalla: ');
	read(s);
	write('[+] Cadena impresa: ');
	writeln(s);
	s := 'cambio de planes morlanes';
	writeln(s);
end;
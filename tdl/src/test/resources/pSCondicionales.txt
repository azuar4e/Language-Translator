procedure holaMundo(var y: integer);
var s : string;
begin
	s := 'holamundo';
    writeln(s);
	y := 13;
    return;	
end;

procedure area(b: integer; h: integer; var res: integer);
begin
	res := b * h;
    return;	
end;	

PROGRAM pruebaSCondicionales;

var x : integer;
	y : integer;
	n : integer;
	b : boolean;
	s : string;

begin
	If x = 1 Then s:= 'x igual que 1';
	if (b) then write('hola mundo');
	holaMundo(y);
	if y = 12 then writeln(y);
	x := 12;	
	if(x = y) then
		begin
			writeln (x);
			if(10 = 10) then return;
		end;
	else 
		begin
			area(5, 10, x);
			write(x);
		end;
end;
{ Se debe editar el fichero para hacer las siguientes modificaciones: 
// + cambiarlo por uno de los operadores aritméticos implementados
// operador relacional: cambiarlo por uno de los operadores relacionales implementados 
// and cambiarlo por uno de los operadores lógicos implementados
// eliminar los operadores especiales no implementado
// en la función sent, dejar solamente el case o el if-else implementado y borrar la otra estructura
}

VAR
 s1: string;
 kk: integer;
 bb: boolean;
 pp: integer;
 jj: integer;


procedure imprime (kk: integer; s1: integer);
begin
	write (kk, ' # ');
	if (0 = s1)	{ Cuando s1 valga 0, esta expresión debe evaluarse como falsa y cuando sea positivo, debe evaluarse como verdadera }
	then
	begin
	end;
	else
	begin
	write (s1);
	end;
	writeln ('.');
end;

procedure  sent (s: string; var rr: integer);
var
	iii: integer;
	ss: string;
begin
	iii:= 7;
	jj:= 4;
	pp:= 1;
	ss:=s;
	read (ss);
	s1:=ss;
	writeln (s, s1);

	if (bb and (iii = kk)) then	{ cambiar la condición, pero conservando un operador relacional y un operador lógico}
	begin
		if (bb) then imprime (iii, 0);
	end;
	else
	begin
		imprime (kk, 0);
	end;
	
	rr:= jj;
end;

procedure  Recursivo (nnn: integer; var rr: integer);
var resul: integer;
begin
	kk:= kk * 2;
	imprime (nnn, kk);
	rr:= 1;
	if (16 = nnn) then	return;	{ poner la condición para que se cumpla tras 3 o 4 iteraciones }
	Recursivo (nnn * 2 * 1, rr);
	resul:= nnn * rr;
	imprime (nnn, kk);
	writeln('el resultado es: ', resul);
	rr:= resul;
end;

procedure operador;	{dejar solo la línea con el operador implementado}
var sss: string;
begin
	writeln ('El minimo es ', MIN (jj, 22, 33, kk, 44, pp*3));
end;

program main;
var cinco: integer;
begin
	cinco:= 5;
	s1:= 'introduce un digito. ';
	bb:= 5 = cinco; { cambiar los valores para que bb siempre valga true en función del operador }
	write (s1);
	read (kk);

	writeln ('Escribe tu nombre. ');
	sent('Hello, ', cinco);

	operador;
	kk:=5;
	Recursivo(jj, cinco);
	imprime (cinco, kk);
	writeln ('Bye, ', s1);
end;